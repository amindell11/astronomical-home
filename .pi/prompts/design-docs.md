---
description: Use Astronomical design docs as primary context
---
Use this design document as primary context before proposing or implementing anything:
`D:\amind\Documents\Obsidian Vault\Astronomical\OVERVIEW.md`

When this document conflicts with other assumptions, follow the document and call out the conflict.

When examining design docs, prefer the copied design-doc workflow assets when available:

Agents/chains:
- `design-pipeline-lite` for fast ideation (research + options)
- `design-pipeline` for full research → design → ready-to-paste docs
- `docs-cleanup` for polishing and consistency updates
- Supporting agents: `obsidian-scout`, `game-designer`, `design-doc-writer`

Skills/tools to use when relevant:
- `obsidian-notes` skill for vault-aware markdown parsing (wikilinks, embeds, aliases, anchors, frontmatter)
- `trello_to_kanban` tool for Trello JSON → Obsidian Kanban markdown
- `kanban_to_trello` tool for Obsidian Kanban markdown → Trello-like JSON

Task: $@