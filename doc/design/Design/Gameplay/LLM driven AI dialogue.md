Dialogue and interactions are procedurally generated using an LLM with configurable traits and access to tools it can trigger (attack, flee, give items).

In effect, it can make decisions about game state based on dialogue proceedings.
