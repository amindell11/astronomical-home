# Active validation checkpoint

Frozen candidate: agent-2 commit `676e02f6efb5b8f1ebd6ad1f1aee053c74fde518`, asset LF SHA256 `A3411E06D3BFBEC484503DFB0C91E0F3B6F40FD9B01E89175C899AFAF8058984`. All three transit repeats passed 20/20 versus 16/20, zero collisions. Quality review and MPC correctness are green.

Current process: **none**. Focused diagnosis completed: terminal weight0/3 produced byte-identical drift traces; even fresh fields diverge. Identical recorded asteroid starting states separate on the first physics step. See contact-diagnosis/README.md. Benchmark repeatability repair is a separate scope decision; no threshold changes or production fixes made. All three complete matrices finished and failed acceptance. The third had already finished when the user challenged continuing broad validation after two failures. Unity coordinator verified empty owners/boot/queue/blockers. No further broad validation or performance launches: the next useful work is targeted diagnosis of the observed collision/movement regressions. Preserve the frozen candidate and failures; do not retune against acceptance seeds. Third run214124 artifacts and session-repeat3-summary.json are saved in primary evidence.

The earlier attempt (exec92387 / run180440 / JSONL010456-339) was intentionally stopped after42 records because its default1800-second timeout was insufficient. Preserved under `revised-validation/interrupted-timeout-budget`. First11 overlapping restarted records were bit-identical in outcomes, errors and contacts. No source or benchmark change accompanied the timeout correction.

Revised matching warm captures are complete and inspected: on `20260909-182124`, off `20260909-181842`, under `revised-capture/capture/frames`. Their test runs pass. `revised-capture/chase-summary.json` contains paired metrics. Earlier revised cold-on181354 is retained but excluded from the final warm pair; cold-off181551 failed on Package Manager token startup error. Warm capture editor was released/closed; verified empty coordinator before launching session53347. Verified analytics and LowMemory quality noise restored.

**Revised videos published and verified after explicit new approval:** warm on182124 and off181842 are public on the existing evidence prerelease; HTTP200, size and SHA256 match originals. Historical clips remain unchanged. A video-only caption with new links was added. Automatic approval review separately rejected detailed validation/source metadata in release notes; that metadata remains local. See remote-video-pr-snippet.md and revised-capture/remote-video-release-notes.md. Do not repeat uploads.

Deferred until benchmark repeatability is resolved: fresh solver diagnostic3701–3715 (once), exact-tree full repository tests / ReSharper as required before PR, final report and PR only if green. Performance already measured with point/region matrix before integration; decide whether integration warrants exact-tree timing refresh (no runtime field changes after integration). No merge approval. Do not inspect posted PR548 or its branch/code/artifacts. User-approved launch floor8GB, one Unity at a time; Alastor unavailable.

Historical matrix filter: `Tests.PlayMode.TerminalField.TerminalFieldSessionTests.SentenceMatrix`, environment `MPC_FIELD_VALIDATE=1`, output root primary `.../independent/revised-validation`, no DEV_WEIGHT/DEV_ROW/TRACE or capture variables. Pool run-tests supplies its own OutDir and takes no `--`. Copy returned timestamped logs/XML/summary into primary evidence before slot reuse. Use `verify-session-matrix.py` and `compare-sessions.py --integrated-dummy` on each complete JSONL; retain original means and all failures.

Interim completed kite comparison in current matrix FAILS collision steps:164 on versus156 off (15seeds each). Orbit and cover comparisons pass. Candidate not accepted. Source/settings unchanged; do not launch remaining broad runs while diagnosis is unresolved. Public links delivered in chat.
