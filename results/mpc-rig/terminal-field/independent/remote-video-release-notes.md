Failed candidate 1: independent terminal-field chase evidence. This is an evidence-only prerelease, not a software release. The candidate fails acceptance: the frozen transit benchmark achieved only +2 arrivals over baseline instead of the required +4, and chase collision steps increased.

The two videos show a 40-second comparison with the same dense fleeing-chase setup and seed 3301. Native debug gizmos are enabled.

- `20260909-041526-terminal-field-dense-chase-on.mp4`: terminal field on.
- `20260909-041658-terminal-field-dense-chase-off.mp4`: terminal field off.

This tag points at the baseline main commit, not the uncommitted experimental implementation. Only the two videos are attached; implementation details and subsequent debugging belong in the eventual PR.
