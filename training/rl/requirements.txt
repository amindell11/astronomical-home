# Paired with Unity package com.unity.ml-agents 4.0.3 (release_23) per its Installation.md.
# Python must be 3.10.12 exactly (release-pair constraint is >=3.10.1,<=3.10.12) — see .python-version.
mlagents==1.1.0
# mlagents imports pkg_resources, removed in setuptools >= 81.
setuptools<81
# torch >= 2.9 routes torch.onnx.export through the dynamo/onnxscript path, which breaks
# mlagents 1.1.0 checkpoint export; ~2.2.1 is the pairing named by the package docs.
# For CUDA on Windows: pip install torch~=2.2.1 --index-url https://download.pytorch.org/whl/cu121
torch~=2.2.1
